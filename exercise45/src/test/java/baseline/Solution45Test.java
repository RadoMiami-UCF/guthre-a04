/*
 *  UCF COP3330 Fall 2021 Assignment 4 Solution
 *  Copyright 2021 Kimari Guthre
 */
package baseline;

import org.junit.jupiter.api.Test;

import java.nio.file.Path;

import static org.junit.jupiter.api.Assertions.*;

class Solution45Test {

    /*This test was commented out because it tests a method that is now private. Make the method public to use the test!
    @Test
    void testSafeGetPath() {
        assertEquals(Solution45.safeGetPath("lemon"), Path.of("data/lemon.txt"),
                ".txt file extension not being added!");
        assertEquals(Solution45.safeGetPath("apple.html"), Path.of("data/apple.html"),
                "File extension not being preserved!");
        assertEquals(Solution45.safeGetPath("potato/carrot.docx"), Path.of("data/carrot.docx"),
                "Extra parent folders not being trimmed off properly!");
    }*/
}